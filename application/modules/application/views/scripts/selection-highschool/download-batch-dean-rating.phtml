<?php 

$output ='';
$output .="no,transaction_id,no peserta,nama,status,rank"."\n";
$no=1;
foreach($this->applicant as $entry){
	 $output .= $no.','.$entry["transaction_id"].','.$entry["applicantID"].",".$entry["appl_fname"].' '.$entry["appl_mname"].' '.$entry["appl_lname"]."\n";
$no++;
}
		
/*Now we're ready to create a file. This method generates a filename based on
 the current date & time.
*/
$filename = "batch_dean_rating_".date("Y-m-d_H-i",time());

//Generate the CSV file header
header("Content-type: application/vnd.ms-excel");
header("Content-disposition: csv" . date("Y-m-d") . ".csv");
header("Content-disposition: filename=".$filename.".csv");

//Print the contents of out to the generated file.
print $output;

exit;
?>